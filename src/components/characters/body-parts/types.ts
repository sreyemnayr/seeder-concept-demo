export interface HumaaanBodyPartProps {
  /** Skin tone for the face/neck */
  skinColor?: string;
  /** Primary color for part (hair / jacket / etc) */
  primaryColor?: string;
  /** Secondary color for part (wrap / undershirt / shoes / etc) */
  secondaryColor?: string;
  /** Tertiary color for part (only used for sitting characters) */
  tertiaryColor?: string;
}
